# Public skills

Reusable OpenCode skills live in directories containing `SKILL.md`. `report-writing/template/` contains files to copy into a new report folder. Configure OpenCode's `skills` array to point at this checkout to make the skill available from any project.
