[project]
name = "report"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["matplotlib>=3.8", "typer>=0.12"]

[dependency-groups]
dev = ["pyright>=1.1", "ruff>=0.12"]

[tool.uv]
package = false

[tool.ruff]
preview = true
